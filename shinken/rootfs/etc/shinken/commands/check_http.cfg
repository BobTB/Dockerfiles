define command {
    command_name check_http
    command_line $NAGIOSPLUGINSDIR$/check_http -H  $HOSTADDRESS$ -u $URI$ -p $PORT$
}

define command {
    command_name check_https
    command_line $NAGIOSPLUGINSDIR$/check_http -S -H $HOSTADDRESS$ -u $URI$ -p $PORT$ 
}
